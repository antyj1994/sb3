spring:
  datasource:
    url: jdbc:h2:mem:sb3;DB_CLOSE_DELAY=-1;NON_KEYWORDS=KEY,VALUE
    driverClassName: org.h2.Driver

hibernate:
  dialect: org.hibernate.dialect.H2Dialect
  hbm2ddl:
    auto: create

server:
  port: 8080